export enum ChartTypeEnum {
  Table = 'table',
  // if remove _ then check for split('_') in code
  ELine = 'e_line',
  EBar = 'e_bar',
  EScatter = 'e_scatter',
  // EBubble = 'e_effect_scatter',
  EPie = 'e_pie',
  EHeatMap = 'e_heatmap',
  ETreeMap = 'e_treemap',
  EGauge = 'e_gauge'
  // ERadar = 'e_radar',
}
