export enum FileExtensionEnum {
  View = '.view',
  Model = '.model',
  Store = '.store',
  Report = '.report',
  Dashboard = '.dashboard',
  Chart = '.chart',
  Udf = '.udf',
  Yml = '.yml',
  Md = '.md'
}
