export enum FileExtensionEnum {
  Api = '.api',
  Dashboard = '.dashboard',
  Md = '.md',
  Metric = '.metric',
  Model = '.model',
  Report = '.report',
  Udf = '.udf',
  View = '.view',
  Chart = '.chart',
  Yml = '.yml'
}
