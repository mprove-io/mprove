export enum FileExtensionEnum {
  View = '.view',
  Model = '.model',
  Mod = '.mod',
  Store = '.store',
  Report = '.report',
  Dashboard = '.dashboard',
  Chart = '.chart',
  Udf = '.udf',
  Yml = '.yml',
  Md = '.md'
}
