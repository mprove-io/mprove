export enum FileExtensionEnum {
  View = '.view',
  Model = '.model',
  Malloy = '.malloy',
  Store = '.store',
  Report = '.report',
  Dashboard = '.dashboard',
  Chart = '.chart',
  Udf = '.udf',
  Yml = '.yml',
  Md = '.md'
}
