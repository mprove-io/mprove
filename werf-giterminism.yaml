giterminismConfigVersion: 1
config:
  allowUncommitted: true
  dockerfile:
    allowUncommittedDockerignoreFiles: 
    - tools/data/mprove-demo-p_db.dump
helm:
  allowUncommittedFiles:
  - tools/data/mprove-demo-p_db.dump