services:

  disk:
    command:
    - yarn 
    - serve:disk 

  backend:
    command:
    - yarn 
    - serve:backend 

  backend-scheduler:
    command:
    - yarn 
    - serve:backend 

  blockml-single:
    command:
    - yarn 
    - serve:blockml 

  blockml-main:
    command:
    - yarn 
    - serve:blockml 

  blockml-worker:
    command:
    - yarn 
    - serve:blockml 
