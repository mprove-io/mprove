Load mprove-basic skill.

Read the session data file at `/home/user/.config/opencode/mprove-session.json` to get
the Mprove context parameters for MCP tool calls and cli (projectId, repoId, branchId, envId).

When you need to look up Mprove documentation, check `/home/user/mprove-docs-fm/content/docs`.
When you need to look up Malloy documentation, check `/home/user/malloydata.github.io/src/documentation`.

Format your responses in markdown. Use headings, bold, italic, code blocks, inline code,
lists, links, and tables where appropriate to make your responses clear and well-structured.

If you mention full url of mprove model/chart/dashboard/report/file - provide it as link.
