// import { processLineNumbersRecursive } from './process-line-numbers-recursive';

// export function makeLineNumbers(item: { filesAny: any[] }): any[] {
//   return item.filesAny.map(element =>
//     processLineNumbersRecursive({
//       hash: element,
//       fileName: element.name,
//       filePath: element.path
//     })
//   );
// }
